# 标准智能助手当前执行开发计划（逐步明细）

## 1. 文档信息
- 项目：标准智能助手（Web 单页问答版）
- 文档类型：执行跟踪计划（按实际开发步骤记录）
- 版本：v1.0
- 更新时间：2026-02-28
- 负责人：当前开发会话（前后端联动）

## 2. 当前状态总览
- 当前阶段：MVP 核心链路打通阶段
- 前端状态：可发起流式问答、可显示 Markdown 格式回答、可显示后端连接状态
- 后端状态：FastAPI + LangChain + DeepSeek 已接入，支持流式输出和会话记忆（Memory）
- 已完成接口：
  - `GET /api/v1/health`
  - `POST /api/v1/chat`
  - `POST /api/v1/chat/stream`（SSE）

## 3. 分步骤执行记录（真实进度）

### Step 1（已完成）：项目结构拆分为前后端独立工程
目标：
- 将前端与后端拆分，避免代码耦合，支持独立开发和联调。

具体操作：
1. 创建 `frontend/` 与 `backend/` 目录。
2. 创建根目录 `docker-compose.yml`，编排 `web/app/postgres/redis`。
3. 创建根目录 `README.md`，提供一键启动说明。

产出文件：
- `/Users/lfc/Documents/New project/frontend/*`
- `/Users/lfc/Documents/New project/backend/*`
- `/Users/lfc/Documents/New project/docker-compose.yml`
- `/Users/lfc/Documents/New project/README.md`

验收方式：
- 执行 `docker compose up --build` 可启动容器。

---

### Step 2（已完成）：后端 API 骨架搭建
目标：
- 提供可联调的基础后端服务。

具体操作：
1. 使用 FastAPI 创建入口服务。
2. 新增健康检查与聊天接口骨架。
3. 增加请求/响应模型，统一响应字段结构。

产出文件：
- `/Users/lfc/Documents/New project/backend/app/main.py`
- `/Users/lfc/Documents/New project/backend/app/api/health.py`
- `/Users/lfc/Documents/New project/backend/app/api/chat.py`

验收方式：
- `GET /api/v1/health` 返回 `{"status":"ok"}`。
- `python3 -m compileall backend/app` 通过。

---

### Step 3（已完成）：前端单页聊天界面搭建
目标：
- 实现 Web 单页对话 UI，支撑后端联调。

具体操作：
1. 基于 React + Vite + TypeScript 初始化前端工程。
2. 实现消息列表、输入框、发送按钮。
3. 增加会话 ID 本地保存逻辑（`session_id`）。
4. 增加后端连接检测与状态展示。

产出文件：
- `/Users/lfc/Documents/New project/frontend/src/App.tsx`
- `/Users/lfc/Documents/New project/frontend/src/services/api.ts`
- `/Users/lfc/Documents/New project/frontend/src/styles.css`
- `/Users/lfc/Documents/New project/frontend/vite.config.ts`

验收方式：
- `npm --prefix frontend run build` 通过。
- 页面可发请求并展示后端返回内容。

---

### Step 4（已完成）：接入 LangChain + DeepSeek 问答
目标：
- 用真实大模型替换占位回复，实现可用问答。

具体操作：
1. 增加模型配置读取（`DEEPSEEK_API_KEY`、模型名、超时、温度）。
2. 新增 `qa_service` 封装 LangChain 调用逻辑。
3. `chat` 路由改为调用服务层。
4. 配置错误与上游模型错误分开处理（500/502）。

产出文件：
- `/Users/lfc/Documents/New project/backend/app/core/settings.py`
- `/Users/lfc/Documents/New project/backend/app/services/qa_service.py`
- `/Users/lfc/Documents/New project/backend/app/api/chat.py`
- `/Users/lfc/Documents/New project/backend/.env.example`
- `/Users/lfc/Documents/New project/backend/requirements.txt`

验收方式：
- 配置有效 `DEEPSEEK_API_KEY` 后，`/api/v1/chat` 可返回模型回答。

---

### Step 5（已完成）：实现 SSE 流式输出
目标：
- 前端可边接收边渲染模型输出，提高交互体验。

具体操作：
1. 后端新增 `POST /api/v1/chat/stream`。
2. SSE 事件定义：
   - `meta`：请求元信息（`trace_id`）
   - `delta`：增量文本分片
   - `done`：完整收尾结果
   - `error`：错误事件
3. 前端新增流式解析器，按事件更新消息内容。
4. 新增“生成中”状态样式，避免用户误判卡死。

产出文件：
- `/Users/lfc/Documents/New project/backend/app/api/chat.py`
- `/Users/lfc/Documents/New project/frontend/src/services/api.ts`
- `/Users/lfc/Documents/New project/frontend/src/types/chat.ts`
- `/Users/lfc/Documents/New project/frontend/src/App.tsx`

验收方式：
- 前端提问时，回答逐步输出，不是一次性返回。

---

### Step 6（已完成）：前端 Markdown 格式渲染
目标：
- 解决“后端返回 Markdown，前端仅按纯文本显示”的问题。

具体操作：
1. 新增内置 `MarkdownContent` 渲染组件（零外部依赖）。
2. 支持标题、列表、粗体、行内代码、代码块、表格、引用、链接。
3. 前端助手消息改为 Markdown 渲染，用户/系统消息保持纯文本。
4. 增加 Markdown 专用样式，保证排版可读性。

产出文件：
- `/Users/lfc/Documents/New project/frontend/src/components/MarkdownContent.tsx`
- `/Users/lfc/Documents/New project/frontend/src/App.tsx`
- `/Users/lfc/Documents/New project/frontend/src/styles.css`

验收方式：
- 带 Markdown 的回答可按格式显示，不再“挤成纯文本”。
- `npm --prefix frontend run build` 通过。

---

### Step 7（已完成）：LangChain Memory 对话记忆
目标：
- 同一会话可记住前文，实现多轮上下文问答。

具体操作：
1. 在服务层引入 `RunnableWithMessageHistory`。
2. 使用 `MessagesPlaceholder("history")` 注入对话历史。
3. 会话键规则：`user_id:session_id`。
4. 非流式与流式接口都切到同一记忆链路。
5. 增加内存保护：每会话最多保留最近 40 条消息。

产出文件：
- `/Users/lfc/Documents/New project/backend/app/services/qa_service.py`
- `/Users/lfc/Documents/New project/backend/app/api/chat.py`
- `/Users/lfc/Documents/New project/backend/README.md`

验收方式：
- 使用同一 `session_id` 连续提问，第二问能引用第一问上下文。
- 更换新 `session_id` 后，记忆重置。

## 4. 当前“正在做”的事项（进行中）

### Step 8（进行中）：整理执行型文档并固化交付节奏
目标：
- 把当前真实开发动作落到执行文档，便于后续按步骤推进和复盘。

当前动作：
1. 完成本文档（逐步明细、文件级产出、验收方式）。
2. 对齐现有三份计划文档与当前真实进度。
3. 明确下一阶段具体开发清单（见第5章）。

## 5. 下一阶段开发清单（明确到可执行）

### Step 9（已完成-代码侧）：将 Memory 从内存迁移到 Redis
目标：
- 解决服务重启后记忆丢失问题，支持多实例共享会话历史。

已执行动作：
1. 新增 Redis 历史存储类 `RedisChatMessageHistory`。
2. 替换 `_history_store` 本地字典为 Redis 存储实现。
3. 增加会话过期策略（`MEMORY_TTL_SECONDS`）。
4. 增加会话消息上限（`MEMORY_MAX_MESSAGES`）。
5. Memory key 增加前缀配置（`MEMORY_KEY_PREFIX`）。
6. 更新后端依赖与 README 说明。

完成标准：
- 重启后端后，历史会话仍可继续。
- 同一会话在不同实例也能读到同样历史。

当前阻塞：
- 当前开发环境无法访问外网仓库，`redis` Python 包未能安装完成。
- 代码已落地，待执行 `pip install -r requirements.txt` 后可直接启用。

---

### Step 10（待开始）：接入标准知识库检索（RAG）
目标：
- 当前回答依赖通用模型，需切换到“可引用标准条款”的问答链路。

计划动作：
1. 设计并创建 `standards`、`standard_clauses` 数据表。
2. 完成标准正文切片脚本（章/节/条结构化）。
3. 接入 embedding 与 pgvector 检索。
4. 在回答中返回真实 `citations`（标准编号/版本/条款）。

完成标准：
- 回答可提供标准条款引用，不再是空 `citations`。

---

### Step 11（待开始）：实现对话内版本比对链路
目标：
- 在聊天中支持“标准 A 版本 vs B 版本”的结构化对比。

计划动作：
1. 识别 `version_compare` 意图。
2. 实现条款对齐与差异抽取（新增/删除/修改）。
3. 前端对比结果分块渲染（摘要 + 差异列表）。

完成标准：
- 聊天中输入比对指令，返回结构化差异结果。

---

### Step 12（待开始）：反馈、评测与上线保障
目标：
- 建立闭环，确保可持续优化。

计划动作：
1. 接入 `POST /api/v1/feedback`。
2. 建立基础指标页：响应时延、错误率、满意度、引用率。
3. 增加压测与告警（5xx、超时、模型失败率）。
4. 完成灰度上线与回滚方案。

完成标准：
- 能按天查看指标并定位问题。
- 发布前有完整检查清单。

## 6. 每日执行模板（从今天开始使用）

`[日期] [步骤编号] [状态]`
- 今日目标：
- 实际改动文件：
- 执行命令：
- 验证结果：
- 阻塞问题：
- 明日动作：

示例（2026-02-28）：
- 今日目标：完成 Memory 接入、Markdown 显示修复与 Redis 持久化
- 实际改动文件：
  - `backend/app/services/qa_service.py`
  - `backend/app/services/redis_history.py`
  - `backend/app/api/chat.py`
  - `backend/app/core/settings.py`
  - `backend/requirements.txt`
  - `backend/.env.example`
  - `frontend/src/components/MarkdownContent.tsx`
  - `frontend/src/App.tsx`
- 执行命令：
  - `python3 -m compileall backend/app`
  - `npm --prefix frontend run build`
- 验证结果：前后端构建通过，流式问答可用
- 阻塞问题：网络不可达 pip registry，暂未安装 `redis` 包
- 明日动作：安装后端依赖并做 Redis Memory 端到端验证
